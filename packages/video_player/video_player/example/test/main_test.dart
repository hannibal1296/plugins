import 'package:test/test.dart';
import 'package:pyunhai_woondng_demo/cliplist.dart';

void main() {
  test('Init workoutCourse', () {
    var mockCourse = WorkOutCourse();
    assert(mockCourse != null);
  });
}
